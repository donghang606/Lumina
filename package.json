{
  "name": "lumina",
  "private": true,
  "scripts": {
    "dev:desktop": "pnpm --filter @lumina/desktop dev",
    "dev:server": "pnpm --filter @lumina/server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@11.18.0",
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^26.15.3",
    "tsup": "^8.5.1"
  }
}